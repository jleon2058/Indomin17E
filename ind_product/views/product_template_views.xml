<?xml version="1.0" ?>
<odoo>
    <!-- Herencia de Vista Formulario de Product Template -->
    <record id="ind_product_template_form" model="ir.ui.view">
        <field name="name">product.template.view.form.inherit.ind_product</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='standard_price']" position="attributes">
                <attribute name="groups">ind_product.group_product_price</attribute>
            </xpath>
        </field>
    </record>

    <record id="ind_product_template_only_form" model="ir.ui.view">
        <field name="name">ind.product.template.form</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_only_form_view"/>
        <field name="arch" type="xml">
            
            <xpath expr="//field[@name='default_code']" position="before">
                <field name="is_admin" invisible="1"/>
            </xpath>
            <xpath expr="//field[@name='default_code']" position="attributes">
                <attribute name="readonly">is_admin == False</attribute>
            </xpath>

        </field>
    </record>

    <!-- Herencia de Vista Tree de Product Template -->
    <record id="ind_product_template_tree" model="ir.ui.view">
        <field name="name">product.template.view.tree.inherit.ind_product</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_tree_view" />
        <field name="arch" type="xml">
            <xpath expr="//field[@name='standard_price']" position="attributes">
                <attribute name="groups">ind_product.group_product_price</attribute>
            </xpath>
        </field>
    </record>
    
</odoo>
